server:
  port: 8083

spring:
  messages:
    basename: messages
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082/realms/selmag

  data:
    mongodb:
      uri: mongodb://localhost:27017/feedback


